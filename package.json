{
  "scripts": {
    "preinstall": "npm install axios",
    "postinstall": "npm install dns-sync"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dns-sync": "^0.2.1",
    "readline-sync": "^1.4.10"
  }
}
